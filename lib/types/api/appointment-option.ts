/**
 * Represents an option for an appointment, typically used to display selectable choices.
 *
 * @property id - The unique identifier for the appointment option.
 * @property name - The display name of the appointment option.
 */
export interface AppointmentOption {
  id: string;
  name: string;
}
