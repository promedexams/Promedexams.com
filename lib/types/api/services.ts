/**
 * Represents a response object for a service type.
 *
 * @property id - The unique identifier of the service type.
 * @property name - The display name of the service type.
 */
export interface ServiceTypeResponse {
  id: string;
  name: string;
}
